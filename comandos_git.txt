git init      -> cria um repo git na pasta atual
git status    -> mostra o status do trabalho (de cada arquivo)
git add <nome arquivo> -> adiciona arquivo para ser commitado
git commit -m "msg" -> faz commit do que estava para ser commitado
git log       -> mostra todo o histórico de commit
pwd    -> aonde estou
git diff <arquivo>   -> 
git checkout -b <nova branch>   -> criar nova branch, clone do projeto
git branch -> mostra todas as branch e qual vc está
git checkout <branch>   -> mudar de branch
git merge <outra branch>  -> 




## FLUXO DE TRABALHO
1. criar o diretório (pasta) do projeto
2. iniciar o git dentro do diretório criado (git init .)
3. edita ou cria arquivos
4. git status
5. git add <nome arquivo>
6. git status
7. git commit -m "mensagem"
8. git status
9. volta para o passo 3.